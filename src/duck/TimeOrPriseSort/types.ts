const SET_FILTER_BY_PRICE = 'app/duck/TimeOrPriseSort/SET_FILTER_BY_PRICE';
const SET_FILTER_BY_TIME = 'app/duck/TimeOrPriseSort/SET_FILTER_BY_TIME';

const types = {
  SET_FILTER_BY_PRICE,
  SET_FILTER_BY_TIME,
};

export default types;
